import { useEffect, useState } from 'react'
import { Ban, CheckCircle, Trash2, UserPlus } from 'lucide-react'
import { Avatar, Badge, Button, Card, PageHeader } from '@/components/UI'
import userService from '@/services/userService'
import { formatDate } from '@/utils/helpers'
export default function AdminUsers(){const [users,setUsers]=useState([]);const [q,setQ]=useState('');useEffect(()=>{userService.getUsers({search:q}).then(r=>setUsers(r.users))},[q]);return <div><PageHeader title="Users Management" subtitle="Manage all platform users." action={<Button><UserPlus size={16}/>Add User</Button>}/><Card><div className="field" style={{maxWidth:380}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search users..."/></div><div className="table-wrap"><table className="table"><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Rating</th><th>Joined</th><th>Status</th><th>Actions</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><div style={{display:'flex',gap:10,alignItems:'center'}}><Avatar name={u.name}/>{u.name}</div></td><td>{u.email}</td><td><Badge variant={u.role==='admin'?'primary':'secondary'}>{u.role}</Badge></td><td>{u.rating?.toFixed(1)} ({u.reviewsCount})</td><td>{formatDate(u.createdAt)}</td><td><Badge variant={u.isBlocked?'danger':'success'}>{u.isBlocked?'Blocked':'Active'}</Badge></td><td><div style={{display:'flex',gap:8}}><Button size="sm" variant="outline">{u.isBlocked?<CheckCircle size={14}/>:<Ban size={14}/>}</Button><Button size="sm" variant="outline"><Trash2 size={14}/></Button></div></td></tr>)}</tbody></table></div></Card></div>}
