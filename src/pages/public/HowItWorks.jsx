import { UserPlus, Lightbulb, GitMerge, CalendarCheck } from 'lucide-react'
import { Card } from '@/components/UI'
const steps=[[UserPlus,'Register','Create your account and set up your profile.'],[Lightbulb,'Add Skills','Add skills you offer and want to learn.'],[GitMerge,'Match','Get suggested people with complementary skills.'],[CalendarCheck,'Start Session','Send a request, schedule time, chat, and learn.']]
export default function HowItWorks(){return <section className="section"><div className="section-inner"><div className="section-title"><h2>How SkillBridge Works</h2><p className="muted">From profile to session in four clear steps.</p></div><div className="grid grid-4">{steps.map(([Icon,t,d],i)=><Card key={t} style={{textAlign:'center'}}><div style={{width:58,height:58,borderRadius:16,margin:'0 auto 14px',display:'grid',placeItems:'center',background:'linear-gradient(135deg,var(--primary),var(--secondary))',color:'#fff'}}><Icon/></div><BadgeNumber n={i+1}/><h3>{t}</h3><p className="muted">{d}</p></Card>)}</div></div></section>}
function BadgeNumber({n}){return <div style={{fontSize:12,fontWeight:900,color:'var(--primary)',marginBottom:8}}>STEP {n}</div>}
